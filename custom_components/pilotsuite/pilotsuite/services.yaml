# PilotSuite Styx Services — HA-184
# Sync mit Core API über /api/v1/*
check_hacs_gate:
  description: Check HACS gate before update
  fields:
    core_url:
      description: Core API URL
      example: "http://localhost:8909"
get_hacs_discovery:
  description: Get HACS discovery metadata
  fields:
    core_url:
      description: Core API URL
      example: "http://localhost:8909"
