name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  validate-repo-shape:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify canonical integration path
        run: |
          test -d custom_components/pilotsuite
          test -f custom_components/pilotsuite/manifest.json
      - name: Verify version consistency
        run: |
          root_v=$(cat VERSION)
          manifest_root_v=$(python3 -c "import json; print(json.loads(open('manifest.json').read())['version'])")
          manifest_component_v=$(python3 -c "import json; print(json.loads(open('custom_components/pilotsuite/manifest.json').read())['version'])")
          test "$root_v" = "$manifest_root_v"
          test "$root_v" = "$manifest_component_v"
          echo "Versions aligned: $root_v"

  test:
    needs: validate-repo-shape
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install deps
        run: pip install -r requirements_test.txt
      - name: Run canonical integration smoke tests
        env:
          PYTHONPATH: ${{ github.workspace }}
        run: |
          pytest tests/test_agent_status_sensor_projection.py tests/test_brain_activity_sensor_projection.py tests/test_neuron_dashboard_sensors_projection.py tests/test_notification_sensor_projection.py -q
