# PilotSuite HA Integration - Dependencies
# Required for local development and CI tests

# Core validation
pydantic>=2.0
voluptuous>=0.13

# HTTP/API
aiohttp>=3.9

# YAML parsing (for tests)
pyyaml>=6.0

# Testing
pytest>=8.0
pytest-asyncio>=0.23

# Home Assistant test helpers (if needed)
# homeassistant>=2024.1
