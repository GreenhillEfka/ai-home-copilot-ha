# Memory Log — 2026-02-24

## Groky Dev Check — Ollama Setup & Cloud Model Integration

**Date:** 2026-02-24  
**User Request:** Install Ollama locally and ensure cloud models (`:cloud` suffix) work via local service.

### Actions Taken

1. **Ollama Installation**
   - Installed Ollama in container via `curl -fsSL https://ollama.com/install.sh | sh`
   - Service started: `ollama serve` (PID 1482)
   - Configuration: `OLLAMA_HOST=http://127.0.0.1:11434`, `OLLAMA_API_KEY=<set>`
   - Local models available: `glm-4.7-flash:latest`, `deepseek-r1:latest`, `codellama:latest`

2. **Cloud Model Testing**
   - Pulled cloud models via `ollama pull`:
     - `qwen3-coder-next:cloud`
     - `kimi-k2.5:cloud`
     - `minimax-m2.5:cloud`
     - `glm-5:cloud`
   - `ollama list` shows all cloud models with IDs
   - API `/api/tags` only shows local models + `glm-5:cloud`
   - `/api/generate` returns "model not found" for `qwen3-coder-next:cloud`, `kimi-k2.5:cloud`, `minimax-m2.5:cloud`
   - `/api/chat` with `glm-5:cloud` works (remote host: ollama.com)

3. **Cloud API Verification**
   - `https://ollama.com/api/tags` with `Authorization: Bearer <key>` returns all models (including `qwen3-coder-next`, `kimi-k2.5`, `minimax-m2.5`, `glm-5`)
   - Cloud models are **remote proxies** – they run on ollama.com, not locally
   - Ollama local server cannot serve `:cloud` models directly (by design)

4. **Cron-Job Configuration**
   - Job ID: `f5fe3986-01a0-49e5-af99-f95ab148e9e6`
   - Name: "Groky Dev Check (10min)"
   - Schedule: `*/10 * * * *` (Europe/Berlin)
   - Session: isolated
   - Model: changed multiple times (`ollama/glm-4.7-flash:latest` → `ollama/glm-5:cloud`)
   - Delivery: telegram to `1616970089`
   - Agent: `grok-4` (configured with `xai/grok-4`, not Ollama)

### Key Findings

- **Cloud models (`:cloud`) are NOT local** – they are remote proxies to ollama.com
- Ollama local server (`127.0.0.1:11434`) cannot serve `:cloud` models directly
- Direct API access to `https://ollama.com/api/chat` with `OLLAMA_API_KEY` works
- Groky Dev Check runs every 10min, checks both repos (pilotsuite-styx-core and -ha)
- Branch: `dev/groky-main`, clean except untracked files → NO PUSH

### Decision

- Groky Dev Check cron job uses `xai/grok-4` (agent default), not Ollama
- Ollama cloud models require direct API calls to `https://ollama.com` with API key
- For local-only development, use `:latest` models (e.g., `glm-4.7-flash:latest`)

### Next Steps

- Monitor cron job status
- Update script if direct Ollama API integration needed (via `requests` to `https://ollama.com`)
- Keep `dev/groky-main` branch clean or push untracked changes

## Groky Dev Check — Agent Fallback Models

**Date:** 2026-02-24  
**User Request:** Configure all agents to use cloud models as fallbacks.

### Actions Taken

1. **Ollama Service Setup**
   - Created startup script `/config/.openclaw/startup.sh` to ensure Ollama starts on reboot
   - Script checks if `ollama serve` is running, starts it if not
   - Sets `OLLAMA_HOST=127.0.0.1:11434` and `OLLAMA_API_KEY=<configured>`

2. **Model Testing**
   - `ollama/qwen3-coder-next:cloud` → ✅ funktioniert (remote host: ollama.com)
   - `ollama/glm-5:cloud` → ✅ funktioniert (remote host: ollama.com)
   - `ollama/kimi-k2.5:cloud` → ❌ nicht gefunden (model not found)
   - `ollama/minimax-m2.5:cloud` → ❌ nicht gefunden (model not found)

3. **OpenClaw Models List**
   - `ollama/minimax-m2.5:cloud` → fallback#1 (local auth: yes)
   - `ollama/qwen3-coder-next:cloud` → fallback#2, configured ✅
   - `ollama/glm-5:cloud` → fallback#3, configured ✅
   - `ollama/deepseek-r1:latest` → fallback#4, configured ✅
   - `ollama/kimi-k2.5:cloud` → image (not working)

4. **Startup Script**
   - File: `/config/.openclaw/startup.sh`
   - Executable: yes
   - Logic: Check if Ollama running → start with nohup → start gateway
   - Ensures Ollama survives reboots/updates

### Key Findings

- **Cloud models (`:cloud`) are remote proxies** to ollama.com via API key
- Only `qwen3-coder-next:cloud` and `glm-5:cloud` work locally over `127.0.0.1:11434`
- `minimax-m2.5:cloud` and `kimi-k2.5:cloud` not accessible via local Ollama server
- OpenClaw Models List shows fallback models configured

### Decision

- Use `ollama/qwen3-coder-next:cloud` or `ollama/glm-5:cloud` as fallback for all agents
- Both models are functional via local Ollama server with API key
- Cloud models run on ollama.com, not locally – but Ollama handles the routing

### Next Steps

- Update all agents to use `ollama/qwen3-coder-next:cloud` as fallback
- Verify OpenClaw agent configuration
- Monitor cron job status and cloud model availability
