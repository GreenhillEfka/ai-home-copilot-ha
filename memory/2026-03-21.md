# Memory — 2026-03-21

- **PilotSuite architecture re-clarified by Andreas (durable):** **HA/HACS** delivers **entities, events, and Home Assistant context** into the **Core Home Assistant module**; **Core** is the semantic/neuronal truth layer that normalizes, categorizes, feeds Brain/Neurons, recognizes **Habitus**, manages zone-state logic, and generates suggestions; **HA** then visualizes that **Core truth** back into dashboards and Habitus-Zonen UI. This is not open for debate.
- **PilotSuite reporting rule sharpened (durable):** always report these separately: **(1) HA/HACS version**, **(2) Core/Add-on version**, **(3) Git/repo state**, **(4) live/runtime state**. Do **not** collapse repo truth into release truth or release truth into live truth.
- **Release-claim guardrail learned on 2026-03-21:** never call PilotSuite **paired / clean / live** unless all four are verified together: **main truth**, **published GitHub release truth**, **runtime health/ports**, and **contract endpoint health**.
- **User workflow preference reinforced:** Andreas should **not** be made the bottleneck for internal confusion. Only ask him for a **single concrete live action** when truly necessary (e.g. restart/update/click), with explicit success criteria.
- **End-of-session published version truth (2026-03-21):** **HA integration via HACS = `15.0.1`**; **Core via Add-on release = `15.0.0`**.
- **End-of-session repo truth (2026-03-21):** Andreas provided screenshot evidence that **Core git/main is ahead of `15.0.0`** (newer commits exist, e.g. `Release v15.0.2 ...`), but that newer git state is **not** the same as the published/live Core add-on version.
- **PilotSuite version truth updated (2026-03-21 late):** **HA = HACS release**, **Core = Add-on release**, **git/main = separate development lane**. Andreas explicitly reinforced that this distinction must be remembered by the whole team and used in all status reports.
- **Published version truth updated (2026-03-21 late):** **HA/HACS = `15.0.1`**, **Core/Add-on release = `15.0.3`**.
