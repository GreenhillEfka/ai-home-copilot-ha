{
  "name": "pilotstack-dashboard",
  "version": "1.0.0",
  "description": "PilotStack dashboard card build (esbuild)",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "build:cards": "node build.mjs --cards"
  },
  "devDependencies": {
    "esbuild": "^0.24.2"
  }
}
